# @monorepo-exmaple/backend

## 1.0.0

### Major Changes

- ## add python backend by Async-FastAPI-MultiDB
  - Add the Python server to the monorepo example project
  - Use `ruff` and `mypy` for code quality management
  - Add `turbo` as the build manager
